import React from 'react'

export const Button = () => {
    return (
        <div>
            This serve to act as a button, which if the user end up being interested in this webite and want to further persue
            what it's to be a volunteer, they can click the button "Volunteer", which woold've lead them to a sign-up and log-in page,
            which they would be able to create an account or log into an existing account. This would be possible using firebase as a
            database to store the user informations, which would be kept safe.
        </div>
    )
}


